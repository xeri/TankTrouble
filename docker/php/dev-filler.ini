; Dev-only. Mounted by docker/docker-compose.dev.yml, never by the default
; stack. The prepended file no-ops unless TT_DEV_FILLER=1, so even a stray
; mount cannot change served bytes on its own.
auto_prepend_file=/usr/local/etc/php/dev-filler.php
