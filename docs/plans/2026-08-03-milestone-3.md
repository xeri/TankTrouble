<!-- Published 2026-08-08 from ~/.claude/plans/begin-milestone-3-of-buzzing-babbage.md.
     Historical record of a completed milestone: do not edit the plan body. -->

> **Status: done** — tag `m3-complete` (phases A–E all landed; see also
> `loadmaze-replay-green`, `gatef-harness-ready`, `derender-complete`,
> `m2-endpoints-complete`). Register row: `docs/PROGRESS.md`.
>
> The four load-bearing findings this plan lists under "User-confirmed
> decisions" and "Load-bearing findings" **are** recorded in `DECISIONS.md`
> (2026-08-03: mazes remodel / gate B redesigned / VARBINARY author key, and
> the wire-format correction inside the remodel entry). Where this plan and
> `DECISIONS.md` differ, `DECISIONS.md` wins — it records what shipped, this
> records what was intended.
# Milestone 3 — writing PHP (TankTrouble rebuild)

## Context

Repo `C:\Users\eth\websites\TankTrouble`. Milestones 1 (skeleton) and 2 (DB seed) are done and tagged (`skeleton-complete`, `seed-complete`, `m3-prep-complete`); gates A/D/S/C1 green (`python -m pytest tests/ -q`, 22 tests); docker pair validated (php:5.6 on 127.0.0.1:8056, mysql:5.5, DB `tanktrouble`). Milestone 3 per guide §6: turn the 501 stubs into reconstructed PHP, provable by gates B (loadMaze replay) and F (render byte-diff), in the order easiest-evidence-first. Era target: 2017-2018.

**User-confirmed decisions (2026-08-03):**
1. **Mazes remodel**: one row per (author, slot), latest capture wins → 672 rows; superseded captures as SQL trailer comments. Supersedes DECISIONS entry 18.
2. **Gate B**: reconstruct the generator (build pairs from DB, port client's naive shuffle, base64) — gate compares decoded pair-multisets + byte-exact notFound + exact outer format + full 672-state coverage by sampling. The guide's "byte-identical 843/843" is unachievable: server provably shuffled per-request (all 24 permutations of `t/n/d/s` in corpus) and 842/843 payloads answer anonymous `c=<random>` requests.
3. **DB include renamed** `srv/includes/db.php` → `srv/includes/rebuild-db.php` (obviously-modern name, settles guide §10.4). Stays M3 DO-NOT-PROMOTE.

**Load-bearing findings from planning (each needs a DECISIONS.md entry when its phase lands):**
- True response format (corrects guide §6.2 comment): body = `r=` + base64(shuffle(`t=<title>&n=<author>&d=<grid>&s=<slot>`)) — `s` INSIDE the base64, `s=1` in 842/843, absent in the notFound payload (`r=bm90Rm91bmQ9dHJ1ZQ==`). No percent-encoding anywhere in corpus.
- Corpus filename `CODE12` = first 12 chars of Wayback sha1-base32 digest of the response body (verified 843/843) — a capture identity, not a user code. Corpus is a time series: 842 bodies → 744 distinct contents → 672 (author, slot) states.
- Request side (`archive/cdx-passes/F_loadmaze200.json`, 17,378 rows): key-set `{userName,a,b}` with userName always literal `"undefined"` → notFound; key-set `{c,a,b}` → random maze. `a/b/c` are cache-busters. 13 anomalous 2018-06-03/04 rows share one digest (`G7SVMWKCBAA3`, body lost, provably not a shuffled payload) — scope out explicitly.
- SAJAX export list is per-page and changes mid-era: six routes emit 33 wrappers after 2017-03-30 (34 with `showShop` before), infirmary emits 2 (`changePassword`, `sendRequest`). Wrapper emission template recovered byte-exact incl. tab/space quirks; 2018 emission order recovered. Library = Sajax (modernmethod 2005), version not yet pinned.
- Zero era-window SAJAX response bodies archived → `showForumPreviews`/`showForumThread` output can never be byte-gated; forumStyles.css (O) class names are the strongest markup constraint.
- Era captures of one route differ in line count → GATE_F_SPEC's positional line-drop comparator needs difflib projection (spec refinement, record in DECISIONS).

---

## Phase A — infra + mazes remodel + loadMaze.php + gate B

The calibration point. Everything else is judged against how this lands.

### A0. Infra (2 commits)
- `docker/docker-compose.yml`: add `MYSQL_USER`/`MYSQL_PASSWORD: ${TT_DB_USER}/${TT_DB_PASSWORD}` to mysql (auto-granted on `tanktrouble`; PHP never gets root); add `environment:` to php: `TT_DB_HOST=mysql`, `TT_DB_NAME=tanktrouble`, `TT_DB_USER`, `TT_DB_PASSWORD`. Update `docker/.env.example` + local `.env`. Both files already on `tests/credscan_allowlist.txt`.
  - **Verify**: `getenv('TT_DB_HOST')` visible under mod_php in php:5.6-apache. If apache scrubs env, fallback `PassEnv` in Dockerfile — record whichever in DECISIONS.
- `git mv srv/includes/db.php srv/includes/rebuild-db.php`. Content: M3 header (DO NOT PROMOTE), `mysql_connect` from `TT_DB_*` env, `mysql_select_db`, `mysql_set_charset('utf8')`, escaping helper. Later invented constants (Phase E saveMaze endpoint) live here — no second invented file. Ledger row renamed in place. DECISIONS entry (name chosen, rejected `db.php`, reversible).

### A1. Mazes remodel (2 commits: code then data — never mixed)
- `seed/seed_mazes.py` rewrite: parse `s` (stop discarding); drop `unquote_plus` (zero `%`/`+` in corpus — verify claim during rewrite); one row per (author, slot), latest fetchts wins; trailer comments: existing notFound line + `-- superseded:` per losing capture + `-- witness:` digest/ts per winning row. Corrected docstring (digest finding, time-series finding).
- `docker/mysql/init/00-schema.sql`: `mazes(author VARCHAR(16), slot TINYINT, title VARCHAR(32), data VARCHAR(512), PRIMARY KEY(author, slot))` — column set M1, author-as-key M2 (deduced from MazeDataFetcher.as userName query), types M3.
- `tests/test_seed.py::test_maze_rows_match_corpus` updated: recompute expected count from corpus by the same latest-wins rule (assert == 672 — verify the number independently here, it came from planning-session decode).
- DECISIONS entry superseding 18. Fresh mysql volume (`docker compose down -v; up -d`), confirm row count.

### A2. `srv/includes/loadMaze.php` (1 commit with test + ledger flip)
Per guide §6.2 template, with the wire-format comment **corrected** (say explicitly the guide's `r=<b64>&s=<slot>` line does not match the corpus):
1. `require_once 'rebuild-db.php'`.
2. base64-decode `$_GET['q']`, split `&`, parse pairs order-independently (client shuffled requests too).
3. Accept exactly key-set `{userName,a,b}` or `{c,a,b}`; `a/b/c` read and discarded. Anything else (missing q, undecodable, mixed/unknown keys) → loud HTTP 400 `RECONSTRUCTION:` message; `@caveat` original malformed-input behaviour unknown, not guessed.
4. userName path: `SELECT title, data, slot FROM mazes WHERE author = <escaped>`; no row → inner payload literal `notFound=true` (single pair — byte-exact after encoding); row → pairs `t,n,d,s`.
5. `c` path: `ORDER BY RAND() LIMIT 1` (selection mechanism unobservable — M3 detail, DECISIONS).
6. `shuffleMessage()` PHP port of the client's naive biased swap-shuffle (`frame_1/DoAction.as`); `@caveat` server's real shuffle unverifiable from output; gate compares multisets.
7. Emit `r=` . base64_encode(inner). No trailing newline (omit `?>`), no explicit Content-Type (headers unarchived — `@caveat`).
8. `@caveat` the 13-row 2018-06-03/04 anomaly.

Ledger: tier stays M1; `verified_by` `—` → `tests/test_loadmaze_replay.py` (this lifts gate D's 501-check). Commit `feat(M1): includes/loadMaze.php + gate B — 672-state replay, notFound byte-exact`.

### A3. `tests/test_loadmaze_replay.py` + live-stack policy
- `tests/conftest.py`: register `live` marker; session fixture `stack()` GETs `http://127.0.0.1:8056/includes/styles.css`, sha256 vs ledger O hash; on failure **`pytest.fail`** (never silent-skip) with instructions: start stack, or run offline gates via `pytest -m "not live"`. Invariant: full suite green with stack up; `-m "not live"` green without docker. DECISIONS entry.
- Tests (`pytestmark = pytest.mark.live`; fixture builds the 672 canonical sorted-pair winner set from the corpus + latest-wins rule):
  - `test_notfound_byte_identical` — ~20 requests, varied key orders + fresh random a/b: body == `b"r=bm90Rm91bmQ9dHJ1ZQ=="` exactly.
  - `test_outer_format` — `^r=[A-Za-z0-9+/]+={0,2}$`, single line, decode→re-encode byte-identical.
  - `test_c_coverage_and_membership` — coupon collector via one persistent `http.client.HTTPConnection`: every decoded response's canonical pair-set ∈ winners; stop at full 672 coverage; hard cap 25,000 requests (expected ≈4.8k); on cap-hit fail listing missing authors. Failure messages mention stale-volume fix (`down -v`).
  - `test_reencode_roundtrip` — each response has exactly keys {t,n,d,s}, s == row slot, canonical form ∈ corpus.
  - `test_deduced_username_lookup` — labelled non-gate sanity (deduced semantics, never archived): `userName=<real author>` returns that author's maze.

**Exit A:** full suite green with stack; `-m "not live"` green without; 672 rows imported; tag `loadmaze-replay-green`. DECISIONS entries: db-include name; mazes remodel (supersedes 18); loadMaze choices (shuffle mirror, ORDER BY RAND(), reject-unknown, headers unknown, 13-row anomaly); live-test fail-not-skip.

---

## Phase B — gate F harness + annotation pass (before any route PHP — guide §7.4a)

### B1. Mask annotation format
5th column `annotation` in `archive-cleaned/classification/<route>.tsv`; vocabulary:
- `echo:$<name>` (+ optional `re=<regex>`, exactly one capture group per echo)
- `loop:<name>` — variable-height block, anchor-mode comparison
- `template-edit:<from-ts>[..<to-ts>]` — literal text with validity window; byte-exact inside window, masked outside. Known dates: phaser/scrapyard 2017-02-21; showShop export removal 2017-03-30; signUpTankDesign 13→17 2017-01-24, 17→18 2018-08-14; sendFeedback removal 2018-04-22; title/keywords edits (date from root captures). **Verify during annotation:** which signUpTankDesign the 2018-03-17 captures embed (agents' reports conflicted 17 vs 18).
- `per-request:<what>` — always masked (incl. date-dependent output; never fake the clock)
- `ad-block` — always masked
- `needs-split:<why>` — blocks gate like an empty annotation
Empty annotation blocks that route's gate (GATE_F_SPEC rule).

### B2. Tooling — `tools/annotate_regions.py` (or extend classify_regions.py)
- `--variants <route> [<region_id>]`: print every distinct byte-form of a region across era captures with timestamps (difflib-aligned) — makes classification mechanical.
- Annotations keyed by (route, region-content-hash) so classifier regeneration preserves them; test asserting annotation-stable regeneration.
- REPORT.md regenerated with annotation-coverage column.

### B3. `tests/test_render_diff.py` (marked `live`, shares `stack` fixture)
- Known inputs per capture: query string (`/` or `/?<route>`), Host header = capture's host column, no cookies.
- Gate-blocking pre-check: unannotated or needs-split region → `pytest.fail` naming route+regions, before any fetch.
- Comparator: **line-mode** default — project mask ranges from reference-line space onto each capture and the render via difflib alignment, drop dynamic lines both sides, byte-compare rest, print first divergent pair. **Anchor-mode** for `loop:` regions — locate static anchors, span must parse as N × item template with regex echo slots (may stay gate-blocking until Phase C's as-of decision).
- Bring-up proof: run end-to-end on **infirmary** first (O file, 0 dynamic regions) — proves plumbing before any PHP exists.
- `tests/test_fenced_regions.py` (DECISIONS-8 obligation): every `/* @O-begin source=… */…/* @O-end */` region in an M* file byte-matches its named source span and is listed in `@contains`. Must exist before the first de-rendered PHP.

**Exit B:** harness green on infirmary; all 217 dynamic regions across root/game/garage/news/forum/lab/shop/embed annotated; regeneration annotation-stable; fenced-region verifier in. Tag `gatef-harness-ready`. DECISIONS: annotation vocabulary + windowing; Host policy; comparator two-mode refinement of GATE_F_SPEC.

---

## Phase C — index.php de-render, six routes + SAJAX generator

- **Structure: single-file `srv/index.php`, no per-route includes** — any new file under srv/ becomes a fetchable path with zero CDX evidence. Shared emitters (tab bar parametrized by route index) as in-file functions, M3 structure. SAJAX library inline as fenced third-party region. DECISIONS entry.
- **SAJAX generator, not hand-written stubs** (guide §6.1a step 4): obtain Sajax 0.10-0.12 sources (archive.org/mirrors; store per archive fetch conventions); diff stock `sajax_get_one_stub()` emission vs recovered byte-exact template (tab/space quirks = fingerprint). Match → version pinned, dispatcher + error format inherited with evidence; no match → site ran modified copy, reimplement emitter to match capture bytes (gate F proves). Dispatcher: `rs=` handled before page emission; unimplemented functions → sajax-native error form, caveated. Export lists: per-route ordered list extracted from each route's reference capture; mid-era changes as `template-edit` annotations.
- **Route order:** game (shell + tab bar + embed + SAJAX block ≈ 80% of every route) → root (44 era captures, richest diff corpus) → lab → shop → garage (hardest template-edit windowing) → news (first DB route) → forum (hardest).
- **DB as-of decision** (GATE_F_SPEC): news — try option (a) `posted <= capture_ts` (items dated, reverse-chron evidenced) + per-item validity for live-edited titles; accept the honest outcome that news may de-render as literal HTML superseding the table (DECISIONS 16 anticipated this). Forum — first check whether any era-captured thread id is missing from the 2026 fetch; if yes, option (a) impossible for listings → hybrid (b) anchor-mode template gating. `showForumPreviews`/`showForumThread` stay **M2** with a labelled shape test (`tests/test_sajax_shape.py`: forumStyles.css class names + modern responses as grade-C hints).
- **Promotion:** index.php `verified_by` → `tests/test_render_diff.py` at first passing route (lifts 501-check; unbuilt routes return loud 501 from inside dispatcher; notes column tracks per-route state). Tier M2→M1 only when root + all six routes pass every era capture; promotion citation = gate F + capture list; DECISIONS per §3.1 rule 4.

**Exit C:** gate F green for root + six routes across all era captures; SAJAX version verdict documented; index.php M1. Tag `derender-complete`.

---

## Phase D — M2 endpoints

- **getScrapyard — two surfaces**: (1) era SAJAX `getScraps` in index.php → JSON `{scraps[, velocity]}` per `srv/includes/scrapyard.js` (O) parse; counter state in an M3 table seeded from the 2016 archived value, labelled arbitrary. (2) pre-era `includes/getScrapyard.php` urlencoded format; `tests/test_getscrapyard_replay.py` replays the two archived bodies (`archive/includes-tree/20150928_getScrapyard.php`, `20160126_…`) with injected state. Tier stays M2 (replay evidence pre-era; M1-on-out-of-era-evidence needs a user decision — flag it then).
- **updateGameStatistics.php — digest check first (free + decisive)**: pull its 2,305 CDX rows; single constant response digest → body recoverable → implement (params from client call sites, M3 stats table, constant body) + mini replay test. Varying digests → stays 501, DECISIONS records why.
- **achievement.php** — no observed response of any kind → stays 501 through m3; DECISIONS entry (decision, not omission).
- **embed.php** — gate F route (masks exist): "dynamic" 82% is deploy-time-constant embed config → echo of constants matching era-final capture; realistic M1 via gate F.
- **getimage.php** — inventory CDX `?id` digests vs held image bytes; implement recoverable id→bytes mapping only; unheld ids → loud reconstruction error. Mostly unrecoverable → stays 501.
- Directory-index stubs (faq/ios/privacy/like/statistics/spreadTheWord/tellAFriendMail/tankRanks/theLabReport): **no capture evidence, stay 501** — DECISIONS entry so it's scope, not neglect.

**Exit D:** scrapyard replay + shape green; updateGameStatistics resolved by digests; embed in gate F; achievement/getimage explicitly resolved. Tag `m2-endpoints-complete`.

---

## Phase E — write-side + auth (deliberately thin, deliberately last)

- `srv/includes/getUserAuthentication.php` + login path: §6.4 deliberate-divergence header verbatim; POST-only + `password_hash`; verifies against synthetic users; minimal session. **Logged-in page rendering NOT reconstructed** (near-zero logged-in captures — would be invention); DECISIONS boundary entry.
- saveMaze constant in `rebuild-db.php` (`TT_SAVE_MAZE_ENDPOINT = 'saveMaze.php'`); DECISIONS entry per guide §3.3's own example; `pending` ledger row for `srv/includes/saveMaze.php` (no file). No endpoint body, no editor — mazeCreator SWF + gate C are the next milestone.
- Write-side SAJAX functions (post, vote, updateTank, moderation set…): remain sajax-native errors; DECISIONS records m3's write-side boundary.

**Exit E / milestone:** auth files POST-only demonstrably; SECURITY.md accurate; tag `m3-complete`.

**Explicitly out of milestone 3:** mazeCreator SWF rebuild, gate C projector, logged-in UI, statistics route (0 era captures), forum SAJAX byte-fidelity (impossible — no archived bodies).

---

## Verification

- Every phase: `python -m pytest tests/ -q` with docker stack up (`docker compose up -d` in docker/, needs `.env`); `pytest -m "not live"` must stay green without docker from Phase A on.
- Gate B: watch it actually iterate — expected ≈4.8k requests, <1 min on localhost keep-alive; failure lists missing authors.
- Gate F: per-route runs during Phase C (`pytest tests/test_render_diff.py -k <route>`); infirmary is the harness smoke test.
- Docker reseed after schema/seed changes: `docker compose down -v && docker compose up -d`, then confirm counts (mazes 672, threads 467, replies 228,316, news 224).
- Ledger discipline mechanics: stub → real file requires `verified_by` naming an existing test in the same commit (gate D enforces both directions).
- Commit style per guide §3.4: tier in message, one file (or tight pair) per commit, never archive bytes + written code together.
